{% extends 'base.html.twig' %}

{% block title %}Welcome home!{% endblock %}

{% block body %}
    <div id="home-container">
        <div class="pt-4 d-flex flex-column justify-content-center align-items-center h-50">
            <img id="home-logo" src="{{ asset('images/give-me-five.svg') }}" class="pb-2 home-appear"  width="70" alt="Main logo">
            <img id="home-name" src="{{ asset('images/main-logo.svg') }}" class="pt-2" width="280" alt="Main project name">
        </div>
        <div id="home-menu" class="w-100 color-white d-flex justify-content-center">
            <div class="d-flex justify-content-around fw-bold flex-column">
                <div id='home-position-button' class="text-center px-3 py-3 m-2 home-menu-items home-appear">From your position</div>
                <div class="text-center px-3 py-3 m-2 home-menu-items home-appear">In big cities</div>
                <div class="text-center px-3 py-3 m-2 home-menu-items home-appear">Custom search</div>
            </div>
        </div>
    </div>
{% endblock %}
